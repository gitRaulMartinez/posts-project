<div 
    class="
        max-w-2xl mx-auto mt-8 flex justify-start items-center
    "
>
    <a
        href="/posts"
        class="
            flex justify-between items-center text-slate-100 text-sm rounded-full 
            bg-slate-800 py-1 px-3 font-medium max-w-[90px]
            hover:bg-slate-900
            focus:ring-2 focus:ring-offset-2 focus:ring-slate-900 mr-2
        "
    >
        <i class="fa-solid fa-chevron-left mr-1"></i>
        <p>Posts</p>
    </a>
    <a
        href="/posts/my-posts"
        class="
            flex justify-between items-center text-slate-100 text-sm rounded-full 
            bg-slate-800 py-1 px-3 font-medium max-w-[110px]
            hover:bg-slate-900
            focus:ring-2 focus:ring-offset-2 focus:ring-slate-900
        "
    >
        <i class="fa-solid fa-chevron-left mr-1"></i>
        <p>Mis posts</p>
    </a>
</div>
<div
    class="
        max-w-2xl bg-white shadow rounded mx-auto mt-2
        max-sm:rounded-none max-sm:max-w-screen-sm max-sm:p-0
    "
>   
    <div class="flex justify-between items-center border-b">
        <h2 class="text-md max-sm:text-sm font-medium leading-6 text-gray-700 px-3 py-5 grow">
            {{post.title}} 
        </h2>
        <div class="flex justify-between flex-none">
            <div class="mr-2 max-sm:hidden">
                <p class="text-sm font-bold text-slate-700 text-end">
                    {{post.user.name}}
                </p>
                <p class="text-sm text-slate-500 text-end">
                    {{post.user.last}}
                </p>
            </div>
            <div class="flex-none px-3">
                {{#if post.user.urlCat}}
                    <img src="{{post.user.url}}" alt="Imagen de perfil" class="bg-white h-10 w-10 rounded-full shadow-md overflow-hidden">
                {{else}}
                    <img src="/images/profile/{{post.user.url}}" alt="Imagen de perfil" class="bg-white h-10 w-10 rounded-full shadow-md overflow-hidden">
                {{/if}}
            </div>
        </div>
    </div>
    <div>
        <div class="flex w-full px-3 py-5">
            <div>
                <p class="text-slate-600 text-sm">
                    {{post.body}}
                </p>
            </div>
        </div>
        {{#if post.image}}
        <div class="mx-auto max-sm:pt-2">
            <img src="/images/posts/{{post.image}}" alt="Imagen de post" class="object-contain max-h-96 mx-auto">
        </div>
        {{/if}}
    </div>
    <div class="w-full px-3 py-5 border-t">
        <p class="text-slate-600 text-sm mb-2"><b>Fecha de publicación:</b> {{post.createdAt}}</p>
        <p class="text-slate-600 text-sm"><b>Última modificación:</b> {{#if post.updatedAt}}{{post.updatedAt}}{{else}}No se realizaron modificaciones{{/if}}</p>
    </div>
</div>
